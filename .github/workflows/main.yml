name: Send Telegram Test Message (INSECURE EXAMPLE - DO NOT USE)
on: [push]

jobs:
  send_test_message:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install python-telegram-bot
        run: pip install python-telegram-bot

      - name: Send Test Telegram Message (INSECURE - but fixing async)
        run: |
          python -c 'import asyncio
from telegram import Bot

async def send_test_message():
    bot = Bot(token="8017759392:AAEwM-W-y83lLXTjlPl8sC_aBmizuIrFXnU")
    chat_id = "711856868"
    await bot.send_message(chat_id=chat_id, text="GitHub Actions Test Message (INSECURE EXAMPLE - FIXED ASYNC)")

if __name__ == "__main__":
    asyncio.run(send_test_message())'
